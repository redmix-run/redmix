// Import other mocking function globals
import '@redmix/testing'
import { InferredCurrentUser } from './all-currentUser'

declare global {
  const mockCurrentUser: (currentUser: InferredCurrentUser) => void
}
